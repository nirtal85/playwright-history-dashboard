{
  "name": "@test-shift/playwright-history-dashboard",
  "version": "0.1.0",
  "description": "Static Playwright execution history dashboard from the native JSON reporter output.",
  "type": "module",
  "bin": {
    "playwright-history-dashboard": "./bin/playwright-history-dashboard.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/playwright-history-dashboard.js && node --check src/playwright-json.js && node --check src/history.js && node --check src/render.js && node --check src/cli.js"
  },
  "keywords": [
    "playwright",
    "test-history",
    "dashboard",
    "github-pages",
    "test-shift"
  ],
  "author": "Test Shift",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ]
}
